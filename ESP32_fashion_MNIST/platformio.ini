; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32-s3-devkitm-1]
platform = espressif32
board = esp32-s3-devkitm-1
framework = espidf
monitor_speed = 115200      
; --- TFLite Micro 关键配置 ---
board_build.partitions = huge_app.csv  ; 使用更大的分区表以容纳你的202KB模型
;    "qio" 是 Flash 模式, "opi" 是 PSRAM 模式
board_build.flash_mode = qio
board_build.arduino.memory_type = qio_opi

build_flags =
    ; ... (your existing flags) ...
    ; ✅ CORRECT LOCATION: Add the FlatBuffers include path here
    -I.pio/libdeps/$PROJECT_ENV_NAME/esp-tflite-micro/third_party/flatbuffers/include

lib_deps =
    https://github.com/espressif/esp-tflite-micro.git
    https://github.com/google/flatbuffers

    
